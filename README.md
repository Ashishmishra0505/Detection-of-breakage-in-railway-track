# Detection-of-breakage-in-railway-track
It"s basically the iot based system which detect the breakage /cracks in the railway tracks to avoid the accidents which is useful in avoiding the rail accidents.
